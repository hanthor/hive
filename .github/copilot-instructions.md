Follow [AGENTS.md](../AGENTS.md): build and test from `src/` (`go build ./...`,
`go test ./...`), sign commits with `git commit -s`, target `v4`, add a
`changelog.d/` fragment for user-visible changes, and never disable a test to
get green.
