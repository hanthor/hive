- `README.md` gained a Tuna OS deployment badge, a `tunaos.org` badge, and a "Tuna OS deployment" section that lists the live hosts (`hub.tunaos.org`, `reef.tunaos.org`, `school.tunaos.org`, `hive.tunaos.org`) with the `/api/health` check that shows each one is up, so an operator finds the fleet from the README without a search. `FORK.md` points at that section ([#3](https://github.com/tuna-os/hive/issues/3)).
